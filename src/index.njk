---
layout: layouts/base.njk
styles:
  - index.css
title: Local Roots. Global Vision.
hero:
  heading: "Waterfront asset. Institutional readiness."
  mission: "We steward waterfront redevelopment with institutional-grade planning, aligning heritage preservation with investor execution."
  ctas:
    primary:
      label: "Explore the Land Opportunity"
      url: "/land-opportunity/"
    secondary:
      label: "Meet Amora Vida"
      url: "/about/"
capabilities:
  heading: "Real estate development rooted in Portuguese stewardship"
  copy:
    - "Amora Vida structures heritage-led land and mixed-use projects across Lisbon’s south bank, pairing municipal partnerships with cross-border capital."
    - "We provide acquisition strategy, planning leadership, and investor reporting so projects move from vision to delivery with discipline."
  cta:
    label: "About Us"
    url: "/about/"
land:
  heading: "Quinta do Paço do Infante"
  preheading: "Current Opportunity"
  description: "190,000 m² waterfront estate with ready documentation for residential, hospitality, and serviced living."
  highlights:
    - "25,000 m² documented GCA spanning premium homes, hospitality, and community amenities."
    - "Loteamento dossier under review; engineering, environmental, and ESG packs complete."
    - "Seixal captures 14% of Greater Lisbon’s population growth with transport upgrades in motion."
    - "Recent waterfront transactions benchmark €3,900–€4,600 €/m², underscoring investor appetite."
  ctas:
    primary:
      label: "View Land Details"
      url: "/land-opportunity/"
    secondary:
      label: "Request Investor Briefing"
      url: "/contact/"
---
<section class="hero">
  <div class="container hero__body">
    {% set taglineParts = site.taglineLines or [site.tagline] %}
    <div class="hero__intro">
      <h1 class="hero__tagline">
        <span class="hero__tagline-line">{{ taglineParts[0] }}</span>
        {% if taglineParts | length > 1 %}
        <span class="hero__tagline-line hero__tagline-line--accent">{{ taglineParts[1] }}</span>
        {% endif %}
      </h1>
      {% if site.brandSlogan %}
      <p class="hero__slogan">{{ site.brandSlogan }}</p>
      {% endif %}
      <p>{{ hero.mission }}</p>
      <div class="hero__chips">
        <span class="hero__chip">190,000 m² waterfront estate</span>
        <span class="hero__chip">Seixal · Lisbon South Bank</span>
      </div>
      <div class="btn-group">
        <a class="btn btn-primary" href="{{ hero.ctas.primary.url }}">{{ hero.ctas.primary.label }}</a>
        <a class="btn btn-secondary" href="{{ hero.ctas.secondary.url }}">{{ hero.ctas.secondary.label }}</a>
      </div>
    </div>
    <figure class="hero__media">
      <img src="/assets/images/LandScapeOverview1.jpg" alt="Aerial view of Quinta do Paço do Infante estate" loading="lazy" width="1280" height="720">
    </figure>
  </div>
</section>

<section class="section">
  <div class="container" style="display:grid;gap:24px;max-width:var(--container-tight);">
    <div class="section-heading">What We Do</div>
    <h2 class="section-title">{{ capabilities.heading }}</h2>
    <div class="prose">
      {% for paragraph in capabilities.copy %}
      <p>{{ paragraph }}</p>
      {% endfor %}
    </div>
    <div>
      <a class="btn btn-secondary" href="{{ capabilities.cta.url }}">{{ capabilities.cta.label }}</a>
    </div>
  </div>
</section>

<section class="section section--surface">
  <div class="container" style="display:grid;gap:32px;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));align-items:start;">
    <div style="display:grid;gap:16px;">
      <div class="section-heading">{{ land.preheading }}</div>
      <h2 class="section-title">{{ land.heading }}</h2>
      <p>{{ land.description }}</p>
    </div>
    <article class="card">
      <strong style="color:#fff;letter-spacing:0.08em;text-transform:uppercase;">Opportunity Highlights</strong>
      <ul style="margin-top:16px;">
        {% for highlight in land.highlights %}
        <li>{{ highlight }}</li>
        {% endfor %}
      </ul>
    </article>
  </div>
  <div class="container" style="margin-top:24px;display:flex;gap:16px;flex-wrap:wrap;">
    <a class="btn btn-primary" href="{{ land.ctas.primary.url }}">{{ land.ctas.primary.label }}</a>
    <a class="btn btn-secondary" href="{{ land.ctas.secondary.url }}">{{ land.ctas.secondary.label }}</a>
  </div>
</section>

<section id="request-briefing" class="section section--primary">
  <div class="container" style="display:grid;gap:24px;max-width:720px;">
    <div>
      <div class="section-heading">Lead Capture</div>
      <h2 class="section-title">Request a Confidential Briefing</h2>
      <p>Share your details to unlock the pack and a leadership follow-up within one business day.</p>
    </div>
    {% include "partials/lead-form.njk" %}
  </div>
</section>
