<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{% if title %}{{ title }} · {% endif %}Amora Vida</title>
  <meta name="description" content="Turnkey waterfront development opportunity at Quinta do Paço do Infante, Amora, Seixal – presented by Amora Vida.">
  <link rel="preload" href="/assets/fonts/20db/20db.woff2" as="font" type="font/woff2" crossorigin>
  {% set pageStyles = styles or [] %}
  <link rel="stylesheet" href="/assets/css/global.css">
  {% for sheet in pageStyles %}
  <link rel="stylesheet" href="/assets/css/{{ sheet }}">
  {% endfor %}
</head>
<body class="theme-light">
  {% include "partials/site-header.njk" %}
  <main>
    {{ content | safe }}
  </main>
  {% include "partials/site-footer.njk" %}
</body>
</html>
